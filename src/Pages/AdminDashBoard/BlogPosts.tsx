import React, { memo } from 'react';

const BlogPosts = memo(() => {
    return (
        <div>
            <p className='pt-9'>This is all posted blogs section</p>
        </div>
    );
});

export default BlogPosts;